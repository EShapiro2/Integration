# Open code work

One line per item: origin (inbox and `Date`), owner, branch, state (requested, forwarded, in progress, merged, rejected), waiting on.  An item leaves when merged or rejected.  Kept by Integration Cowork.

## Worktrees

Created 2026-09-16, one per project with an active Code session; each branch is pushed and tracks `origin/<project>`.  A project not listed has none and asks for one.  The first five were cut at GLP `7dde3845`; GFWC at `c52f6545`, after the harness fix and the Currencies merge (GFWC #1 Cowork, `Integration_inbox.md` 2026-09-16 21:05 UTC).  Legal at `0c9635ef` on 2026-09-18, for its `programs/jurix` task (Legal Cowork, `Integration_inbox.md` 2026-09-18 11:27 UTC); it had none before, having had no code session when the others were cut.  GLP-Spec on 2026-09-18 by Integration #2 Code, for its root `self.glp` task.  `IGLP-manifest`, Integration's branch carrying the sovereign manifest apart from the refusal, was cut 18:06 UTC, merged at `f5aa77f9` and retired.  `IGLP-harness` and `IGLP-display`, off `main` at `fd0dd7e6`, each carry one IGLP task that must not sit on `IGLP`, which holds the refusal (Code, 2026-09-18).

| Project | Worktree | Branch |
|---|---|---|
| IGLP | `/Users/udi/Grassroots/GLP-worktrees/IGLP` | `IGLP` |
| GSG | `/Users/udi/Grassroots/GLP-worktrees/GSG` | `GSG` |
| Currencies | `/Users/udi/Grassroots/GLP-worktrees/Currencies` | `Currencies` |
| vGLP | `/Users/udi/Grassroots/GLP-worktrees/vGLP` | `vGLP` |
| GLP-Networking-API | `/Users/udi/Grassroots/GLP-worktrees/GLP-Networking-API` | `GLP-Networking-API` |
| GFWC | `/Users/udi/Grassroots/GLP-worktrees/GFWC` | `GFWC` |
| Legal | `/Users/udi/Grassroots/GLP-worktrees/Legal` | `Legal` |
| GLP-Spec | `/Users/udi/Grassroots/GLP-worktrees/GLP-Spec` | `GLP-Spec` |
| IGLP (the harness path) | `/Users/udi/Grassroots/GLP-worktrees/IGLP-harness` | `IGLP-harness` |
| IGLP (compounds as scalars) | `/Users/udi/Grassroots/GLP-worktrees/IGLP-display` | `IGLP-display` |

The two worktrees of an earlier session under `/Users/udi/Grassroots/tmp/` --- `glp-bundle` and `glp-gate` --- were removed on 2026-09-16, both superseded.

## Items

Open only; a merged or rejected item leaves the list, git holding it.

### The SGC screens, FC 2027 on 24 September

The chain --- Currencies' re-emission, GSG's structural copy, IGLP's sovereign and coins manifests --- is merged on `main` at `f5aa77f9` and pushed, full suite 1995 green, `KNOWN_RED` empty (Code, 2026-09-18 18:35 UTC).  What remains for the screens:

- **`sovereign_ui/3`, the live-person harness** (Integration #2 Code 2026-09-18 17:26 UTC).  Owner: Currencies, forwarded 17:28 UTC --- `programs/currencies` is theirs and the harness is a program, not Dart.  `main_sovereign.dart` names it and nothing of that name exists, and no sovereign source is in the iOS bundle list, so the screen cannot run until it lands.  Tasked by Currencies 18:23 UTC; first in Code's order.  State: with Integration Code.
- **SGC screens appendix** (Currencies Cowork, `Currencies_inbox.md` 2026-09-15 10:45 UTC).  The eight simulator screens into `/Grassroots/SGC/Figs/`, Currencies Code, after all of the above.  Transcript written; `Figs/` does not exist yet.

### One object checked and compiled

- **The refusal, and the 31 tests it turns red** (Integration #2 Code, `Integration_inbox.md` 2026-09-18 17:56 UTC).  IGLP's item 1 is committed on branch `IGLP` at `237a9fe7`: `strictTypes` gone, the `[TYPE WARNING]` branch throws, `:strict` out of the REPL.  Measured against a baseline: `glp_runtime` 743 green to 721 with 27 failures and 5 new; `glp_multiagent` 81 green to 77 with 4.  Every red was already ill-typed and running unchecked.  By owner: Currencies 12 in `bonds_v2/mad_boot/` (routed 18:03 UTC, tasked by Currencies #2 Cowork 18:28 UTC: a declaration, an import and a qualification, no play changed, `bonds_v2_isolate_test.dart` green); IGLP 4 in `programs/tests/`, tasked by IGLP 18:23 UTC after the boot-scope fix (item 0 of that entry: a boot source is checked in the engine's scope, Implementation Notes `6380be6`), which by itself answers the two `send_to_net/1` refusals; CSSN 13 in `cssn/mad_boot/` --- RULED (Udi, 2026-09-18, to Code 18:16 UTC): CSSN stays dormant, its 13 go into `KNOWN_RED` until it wakes, and nobody repairs its files.  The refusal merges when Currencies' 12 and IGLP's 4 are repaired on their branches.  State: with Integration Code.
- **The harness runs a worktree's Dart against `main`'s programs** (Integration #2 Code, same entry).  `glp_multiagent/test/` hardcodes `/Users/udi/Grassroots/GLP/programs` in `roundtrip_isolate_test.dart:19`, `grassapp_scenario_test.dart:18`, `coins_screen_test.dart:97`, `mad_w_clean_test.dart:24,27`, so a branch cannot measure those tests honestly and a `.glp` change on a branch is invisible to them.  IGLP's Dart, asked of them 18:03 UTC; tasked by IGLP 18:23 UTC --- six files, the path derived from the test's own location, written once --- and to be taken before the refusal's counts are quoted.  Branch `IGLP-harness`.  State: with Integration Code.

- **The ruling and its two sites** (Udi, 2026-09-18; in `claude.md` under "Code", Coordination 16:03 UTC).  No clause of the compiled object is exempt from the checks and no diagnostic on a load path is a warning.  `glp_engine.dart:380`, the type warning, is IGLP's item 1 and in flight on branch `IGLP`.  `compiler.dart:142`, SRSW off for linked programs, is item 2; the TGLP sentences asked of GLP-Spec 15:59 UTC are written (GLP #1 Cowork, `Integration_inbox.md` 2026-09-18 18:04 UTC, Moded-Types `4f7027b`): the alias clause threads each argument at its declared polarity as the forwarding clause of "Design" does, and the flat program is the object checked, every clause of it satisfying SRSW.  IGLP implements from those two sentences, forwarded to them verbatim 18:20 UTC; tasked by IGLP 18:23 UTC as item 2, with item 4, the dead `[TYPE ERROR] ... continue` block at `compiler.dart:78--107`, deleted.  State: with Integration Code.
- **A third unchecked site** (Integration Code's subagent, 2026-09-18).  Every `:boot` prints `[TYPE] 10 parameterized procedure(s) unchecked in this program --- no instantiation`, naming `output:send_friend/4`: a polymorphic procedure no call instantiates is compiled unchecked and the fact is a note.  TGLP now says (GLP #1 Cowork 18:04 UTC, Moded-Types `4f7027b`): a parameterised procedure that inspects none of its parameters is checked once against its abstract instance; one that does, and that no call instantiates, is not parametrically well-typed and compilation rejects the program.  The checker is IGLP's; tasked by IGLP 18:23 UTC as item 3, the note becoming the rejection.  State: with Integration Code.
- **The sweep, when the check is on** (Integration, 2026-09-18).  Turning SRSW on will reject clauses beyond `anchor_friend/4`, in several owners' files; IGLP reports the list with file and line and Integration decomposes it, one task per owner, each written by its own Cowork from its paper.  State: waiting on IGLP's item 2.
- **A fixture per check, on every load path** (Integration, 2026-09-18).  The harness has 1979 checks and none asserts that a program which must be rejected is rejected on the path it is loaded by, so a check switched off costs nothing and shows nothing.  One negative fixture per check --- SRSW, types, guards --- as a file, a directory, a linked program and a multi-isolate boot.  Integration's own, after the sweep.

### Queued and blocked elsewhere

- **GLP-Spec's root `self.glp` types** (GLP #1 Cowork, `Integration-Code_inbox.md` 2026-09-18 18:04 UTC).  Three predefined types added to root `programs/self.glp`, `authorise_link/2` retyped to the catalogue's row, `mad_predicates.glp`'s own `GlobalName` dropped and its agent retyped to `Key`; types only, no clause changes; two shadowing hazards to measure.  Branch `GLP-Spec`, worktree cut 18:06 UTC; Code's next spawn after GSG's task (18:15 UTC), the before/after suite being its run on `main` at the merge.  State: with Integration Code.
- **GFWC's descendants task** (GFWC #3 Cowork 2026-09-18 14:24 UTC, amended by their 16:48 UTC).  Queued behind the merges; the subagent pulls the paper, now at `4970e5f`: five definitions moved at `a75e0b3`, and GFWC answered Code's question of 18:05 UTC at 18:28 UTC --- a total order on agents and identities, names compared lexicographically, the descendants renamed `D_{R,v}` and `D_v`.  State: with Integration Code.
- **Legal's jurix compiler** (Legal #3 Cowork 2026-09-18 14:49 and 15:28 UTC).  Items 1, 2 and 4 committed on branch `Legal`, Section JX in the gate; item 3, the compiler, was blocked on Legal's answers to Integration Code's three questions of 16:46 UTC; Legal answered all three 18:16 UTC, Jurix at `387c7a2`, the worked box now printing the four lines the compiler emits, and Code copied the task and its two amendments into its own inbox 18:17 UTC.  State: with Integration Code.
- **GSG's `new_channel` in a guard** (Integration #2 Code to GSG, 2026-09-18 17:13 UTC).  Section 6.3 admits no procedure in a guard; Appendix E's listing of `introduce/2` carries `new_channel(PQCh, QPCh)` there, and `new_channel/2` is a unit clause in the root `self.glp`.  Either 6.3 means something narrower or the appendix is wrong.  GSG holds it a language question and puts it to GLP-Spec; Appendix E's listing, now behind `introduce/2` plus `introduce_step/9`, is proposed after that answer (GSG #1 Cowork to Udi, 2026-09-18).  State: with GLP-Spec, through GSG.
- **Currencies: the stale `*(no, ...)` display declarations** (vGLP #1 Cowork, `Integration_inbox.md` 2026-09-18 18:17 UTC, forwarded to Currencies 18:20 UTC).  vGLP ruled: a display declaration names its clause's volition guard, the deleted siblings' guards are no clause's, so the `display respond_swap *(no, ...)` line goes from the three responders and the `display respond_transfer *(no, ...)` line from the two bond files, nothing replacing them, and each file is re-emitted; an else-branch has no display declaration, now in vGLP Section 6.3.  Non-blocking; the diagnostic for a declaration matching no clause is IGLP's, tasked 18:23 UTC as item 5 of their load-path entry.  State: with Currencies Cowork, to write the task.
- **A compound under default display shows its scalars, in every app** (IGLP #1 Cowork, `Integration-Code_inbox.md` 2026-09-18 18:23 UTC; vGLP's ruling of 18:17 UTC, in `shapiro2026volition` Section 6.3).  `scalarGroups`/`scalarsOf` in `term.dart` extended from the card context to the panel's list and every other unwidgeted value; the tests of every app brought onto it.  Branch `IGLP-display`.  State: with Integration Code.

### `signature/2` replaces `signed/4` and `signed/2` --- three owners

- **The ruling** (Udi, 2026-09-18, through GLP #1 Cowork, `Integration_inbox.md` 18:22 UTC; catalogue at GLP-ICLP-2026 `6d38a0f`).  `signature/2` takes a signed term apart and reports `signed(Key, Hash, Term)` where the signature verifies and `unsigned` otherwise; `Signature ::= signed(Key, Hash, _) ; unsigned`.  Decomposed 18:27 UTC, non-blocking, in dependency order: IGLP the kernel (`body_kernels.dart`, `root_scope.dart`) and `programs/tests/sign_probe/probe.glp`, asked of IGLP Cowork; GLP-Spec root `programs/self.glp` (the type, the wrapper, the two old wrappers gone), written by GLP-Spec and posted to Code `for GLP-Spec`; GSG its five call sites (`core/agent.glp` 376, 389, 403, 481; `core/superapp_plays.glp` 135), asked of GSG Cowork.  Nothing loads between the kernel and the root file, so those two merge together, GSG after.  Both owner tasks are written, in `Integration-Code_inbox.md`: GSG's 18:26 UTC (the five sites, the two-clause dispatch, the `unsigned` branch at each, four plays), written from GLP-Spec's own word to them before my ask; IGLP's 18:31 UTC (the kernel and the probe).  State: with Integration Code, GSG's held until the other two land together.
